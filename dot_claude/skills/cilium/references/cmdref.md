# Cilium - Cmdref

**Pages:** 251

---

## cilium-dbg bpf egress — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_egress/

**Contents:**
- cilium-dbg bpf egress
- Options
- Options inherited from parent commands
- SEE ALSO

Manage the egress routing rules

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf egress list - List egress policy entries

---

## cilium-dbg bpf ipcache match — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipcache_match/

**Contents:**
- cilium-dbg bpf ipcache match
- Options
- Options inherited from parent commands
- SEE ALSO

Retrieve identity for a prefix using an exact match

cilium-dbg bpf ipcache - Manage the IPCache mappings for IP/CIDR <-> Identity

---

## cilium-dbg bpf ipcache — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipcache/

**Contents:**
- cilium-dbg bpf ipcache
- Options
- Options inherited from parent commands
- SEE ALSO

Manage the IPCache mappings for IP/CIDR <-> Identity

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf ipcache delete - Delete an entry for a prefix

cilium-dbg bpf ipcache get - Retrieve identity for an ip

cilium-dbg bpf ipcache list - List endpoint IPs (local and remote) and their corresponding security identities

cilium-dbg bpf ipcache match - Retrieve identity for a prefix using an exact match

cilium-dbg bpf ipcache update - Update an entry for a prefix

---

## cilium-dbg prefilter update — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_prefilter_update/

**Contents:**
- cilium-dbg prefilter update
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg prefilter - Manage XDP CIDR filters

---

## cilium-dbg bpf fs — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_fs/

**Contents:**
- cilium-dbg bpf fs
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf fs show - Show bpf filesystem mount details

---

## cilium-dbg bpf auth list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_auth_list/

**Contents:**
- cilium-dbg bpf auth list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List all authenticated connections between identities

List all authenticated connections between identities

cilium-dbg bpf auth - Manage authenticated connections between identities

---

## cilium-dbg identity — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_identity/

**Contents:**
- cilium-dbg identity
- Options
- Options inherited from parent commands
- SEE ALSO

Manage security identities

cilium-dbg identity get - Retrieve information about an identity

cilium-dbg identity list - List identities

---

## cilium-dbg envoy admin listeners — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_listeners/

**Contents:**
- cilium-dbg envoy admin listeners
- Options
- Options inherited from parent commands
- SEE ALSO

List configured listeners of Envoy Proxy

cilium-dbg envoy admin - Access Envoy Admin Interface

---

## cilium-dbg recorder delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_recorder_delete/

**Contents:**
- cilium-dbg recorder delete
- Options
- Options inherited from parent commands
- SEE ALSO

Delete individual pcap recorder

cilium-dbg recorder - Introspect or mangle pcap recorder

---

## cilium-dbg shell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_shell/

**Contents:**
- cilium-dbg shell
- Options
- Options inherited from parent commands
- SEE ALSO

Connect to the Cilium shell

---

## cilium-dbg bpf srv6 policy — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_srv6_policy/

**Contents:**
- cilium-dbg bpf srv6 policy
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List SRv6 policy entries

List SRv6 policy entries.

cilium-dbg bpf srv6 - Manage the SRv6 routing rules

---

## cilium-dbg bpf policy get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_policy_get/

**Contents:**
- cilium-dbg bpf policy get
- Options
- Options inherited from parent commands
- SEE ALSO

Get contents of a policy BPF map

cilium-dbg bpf policy - Manage policy related BPF maps

---

## cilium-bugtool completion bash — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-bugtool_completion_bash/

**Contents:**
- cilium-bugtool completion bash
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for bash

Generate the autocompletion script for the bash shell.

This script depends on the ‘bash-completion’ package. If it is not installed already, you can install it via your OS’s package manager.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-bugtool completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg envoy admin logging list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_logging_list/

**Contents:**
- cilium-dbg envoy admin logging list
- Options
- Options inherited from parent commands
- SEE ALSO

List logging levels of Envoy Proxy

cilium-dbg envoy admin logging - List and change logging levels of Envoy Proxy

---

## cilium-dbg config — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_config/

**Contents:**
- cilium-dbg config
- Options
- Options inherited from parent commands
- SEE ALSO

Cilium configuration options

cilium-dbg config get - Retrieve cilium configuration

---

## cilium-operator-aws completion bash — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_completion_bash/

**Contents:**
- cilium-operator-aws completion bash
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for bash

Generate the autocompletion script for the bash shell.

This script depends on the ‘bash-completion’ package. If it is not installed already, you can install it via your OS’s package manager.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-aws completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg envoy admin logging — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_logging/

**Contents:**
- cilium-dbg envoy admin logging
- Options
- Options inherited from parent commands
- SEE ALSO

List and change logging levels of Envoy Proxy

cilium-dbg envoy admin - Access Envoy Admin Interface

cilium-dbg envoy admin logging list - List logging levels of Envoy Proxy

cilium-dbg envoy admin logging set - Change logging levels of Envoy Proxy

---

## cilium-dbg envoy admin logging set — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_logging_set/

**Contents:**
- cilium-dbg envoy admin logging set
- Options
- Options inherited from parent commands
- SEE ALSO

Change logging levels of Envoy Proxy

cilium-dbg envoy admin logging - List and change logging levels of Envoy Proxy

cilium-dbg envoy admin logging set global - Change global logging level for all loggers of Envoy Proxy

cilium-dbg envoy admin logging set loggers - Change logging level of a list of loggers of Envoy Proxy

---

## cilium-operator-azure completion zsh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_completion_zsh/

**Contents:**
- cilium-operator-azure completion zsh
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for zsh

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need to enable it. You can execute the following once:

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-azure completion - Generate the autocompletion script for the specified shell

---

## cilium-operator-aws status clustermesh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_status_clustermesh/

**Contents:**
- cilium-operator-aws status clustermesh
- Options
- SEE ALSO

Display status of remote clusters

cilium-operator-aws status - Display status of operator

---

## cilium-agent hive — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_hive/

**Contents:**
- cilium-agent hive
- Options
- SEE ALSO

cilium-agent - Run the cilium agent

cilium-agent hive dot-graph - Output the dependencies graph in graphviz dot format

---

## cilium-operator-alibabacloud metrics list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_metrics_list/

**Contents:**
- cilium-operator-alibabacloud metrics list
- Options
- SEE ALSO

List all metrics for the operator

cilium-operator-alibabacloud metrics - Access metric status of the operator

---

## cilium-dbg metrics — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_metrics/

**Contents:**
- cilium-dbg metrics
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg metrics list - List all metrics

---

## cilium-dbg bpf vtep list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_vtep_list/

**Contents:**
- cilium-dbg bpf vtep list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List VTEP CIDR and their corresponding VTEP MAC/IP

List VTEP CIDR and their corresponding VTEP MAC/IP.

cilium-dbg bpf vtep - Manage the VTEP mappings for IP/CIDR <-> VTEP MAC/IP

---

## cilium-dbg preflight fqdn-poller — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_preflight_fqdn-poller/

**Contents:**
- cilium-dbg preflight fqdn-poller
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Prepare for DNS Polling upgrades to cilium 1.4

Prepare for DNS Polling upgrades to cilium 1.4 by creating a placeholder –tofqdns-pre-cache file that can be used to pre-seed the DNS cached used in toFQDNs rules. This is useful when upgrading cilium with DNS Polling policies where an interruption in allowed IPs is undesirable. It may also be used when switching from DNS Polling based DNS discovery to DNS Proxy based discovery where an endpoint may not make a DNS request soon enough to be used by toFQDNs policy rules

cilium-dbg preflight - Cilium upgrade helper

---

## cilium-dbg — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/index_cilium-dbg/

**Contents:**
- cilium-dbg

---

## cilium-operator-alibabacloud status — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_status/

**Contents:**
- cilium-operator-alibabacloud status
- Options
- SEE ALSO

Display status of operator

cilium-operator-alibabacloud - Run cilium-operator-alibabacloud

cilium-operator-alibabacloud status clustermesh - Display status of remote clusters

---

## cilium-dbg node — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_node/

**Contents:**
- cilium-dbg node
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg node list - List nodes

---

## cilium-dbg bpf bandwidth — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_bandwidth/

**Contents:**
- cilium-dbg bpf bandwidth
- Options
- Options inherited from parent commands
- SEE ALSO

BPF datapath bandwidth settings

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf bandwidth list - List BPF datapath bandwidth settings

---

## cilium-bugtool completion powershell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-bugtool_completion_powershell/

**Contents:**
- cilium-bugtool completion powershell
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for powershell

Generate the autocompletion script for powershell.

To load completions in your current shell session:

To load completions for every new session, add the output of the above command to your powershell profile.

cilium-bugtool completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg fqdn names — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_fqdn_names/

**Contents:**
- cilium-dbg fqdn names
- Options
- Options inherited from parent commands
- SEE ALSO

Show internal state Cilium has for DNS names / regexes

cilium-dbg fqdn - Manage fqdn proxy

---

## cilium-dbg encrypt — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_encrypt/

**Contents:**
- cilium-dbg encrypt
- Options
- Options inherited from parent commands
- SEE ALSO

Manage transparent encryption

cilium-dbg encrypt flush - Flushes the current IPsec state

cilium-dbg encrypt status - Display the current encryption state

---

## cilium-dbg service — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_service/

**Contents:**
- cilium-dbg service
- Options
- Options inherited from parent commands
- SEE ALSO

List services & loadbalancers

cilium-dbg service list - List services

---

## cilium-dbg endpoint labels — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint_labels/

**Contents:**
- cilium-dbg endpoint labels
- Options
- Options inherited from parent commands
- SEE ALSO

Manage label configuration of endpoint

cilium-dbg endpoint - Manage endpoints

---

## cilium-dbg bgp — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bgp/

**Contents:**
- cilium-dbg bgp
- Options
- Options inherited from parent commands
- SEE ALSO

Access to BGP control plane

cilium-dbg bgp peers - List current state of all peers

cilium-dbg bgp route-policies - List configured route policies

cilium-dbg bgp routes - List routes in the BGP Control Plane’s RIBs

---

## clustermesh-apiserver — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/index_clustermesh-apiserver/

**Contents:**
- clustermesh-apiserver

---

## cilium-health — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/index_cilium-health/

**Contents:**
- cilium-health

---

## cilium-dbg bpf bandwidth list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_bandwidth_list/

**Contents:**
- cilium-dbg bpf bandwidth list
- Options
- Options inherited from parent commands
- SEE ALSO

List BPF datapath bandwidth settings

cilium-dbg bpf bandwidth - BPF datapath bandwidth settings

---

## cilium-dbg version — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_version/

**Contents:**
- cilium-dbg version
- Options
- Options inherited from parent commands
- SEE ALSO

Print version information

---

## cilium-dbg prefilter delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_prefilter_delete/

**Contents:**
- cilium-dbg prefilter delete
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg prefilter - Manage XDP CIDR filters

---

## cilium-dbg endpoint list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint_list/

**Contents:**
- cilium-dbg endpoint list
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg endpoint - Manage endpoints

---

## cilium-dbg bpf config list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_config_list/

**Contents:**
- cilium-dbg bpf config list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List all runtime config entries

List all runtime config entries

cilium-dbg bpf config - Manage runtime config

---

## cilium-dbg endpoint health — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint_health/

**Contents:**
- cilium-dbg endpoint health
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg endpoint - Manage endpoints

---

## cilium-dbg recorder — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_recorder/

**Contents:**
- cilium-dbg recorder
- Options
- Options inherited from parent commands
- SEE ALSO

Introspect or mangle pcap recorder

cilium-dbg recorder delete - Delete individual pcap recorder

cilium-dbg recorder get - Display individual pcap recorder

cilium-dbg recorder list - List current pcap recorders

cilium-dbg recorder update - Update individual pcap recorder

---

## cilium-dbg bpf policy — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_policy/

**Contents:**
- cilium-dbg bpf policy
- Options
- Options inherited from parent commands
- SEE ALSO

Manage policy related BPF maps

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf policy add - Add/update policy entry

cilium-dbg bpf policy delete - Delete a policy entry

cilium-dbg bpf policy get - Get contents of a policy BPF map

cilium-dbg bpf policy list - Dump all policy maps

---

## cilium-dbg preflight — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_preflight/

**Contents:**
- cilium-dbg preflight
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Cilium upgrade helper

CLI to help upgrade cilium

cilium-dbg preflight fqdn-poller - Prepare for DNS Polling upgrades to cilium 1.4

cilium-dbg preflight migrate-identity - Migrate KVStore-backed identities to kubernetes CRD-backed identities

cilium-dbg preflight validate-cnp - Validate Cilium Network Policies deployed in the cluster

---

## cilium-dbg bpf srv6 vrf — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_srv6_vrf/

**Contents:**
- cilium-dbg bpf srv6 vrf
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List SRv6 VRF mappings

List SRv6 VRF mappings.

cilium-dbg bpf srv6 - Manage the SRv6 routing rules

---

## cilium-dbg endpoint log — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint_log/

**Contents:**
- cilium-dbg endpoint log
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

View endpoint status log

cilium-dbg endpoint - Manage endpoints

---

## cilium-dbg bpf metrics list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_metrics_list/

**Contents:**
- cilium-dbg bpf metrics list
- Options
- Options inherited from parent commands
- SEE ALSO

List BPF datapath traffic metrics

cilium-dbg bpf metrics - BPF datapath traffic metrics

---

## cilium-operator-aws metrics — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_metrics/

**Contents:**
- cilium-operator-aws metrics
- Options
- SEE ALSO

Access metric status of the operator

cilium-operator-aws - Run cilium-operator-aws

cilium-operator-aws metrics list - List all metrics for the operator

---

## cilium-dbg service list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_service_list/

**Contents:**
- cilium-dbg service list
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg service - List services & loadbalancers

---

## cilium-operator-aws completion zsh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_completion_zsh/

**Contents:**
- cilium-operator-aws completion zsh
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for zsh

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need to enable it. You can execute the following once:

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-aws completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bpf lb — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_lb/

**Contents:**
- cilium-dbg bpf lb
- Options
- Options inherited from parent commands
- SEE ALSO

Load-balancing configuration

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf lb list - List load-balancing configuration

cilium-dbg bpf lb maglev - Maglev lookup table

---

## cilium-dbg completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_completion/

**Contents:**
- cilium-dbg completion
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Output shell completion code

---

## cilium-dbg bpf ipmasq list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipmasq_list/

**Contents:**
- cilium-dbg bpf ipmasq list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List ip-masq-agent CIDRs

List ip-masq-agent CIDRs. Packets sent from pods to IPs from these CIDRs avoid masquerading.

cilium-dbg bpf ipmasq - ip-masq-agent CIDRs

---

## cilium-dbg debuginfo — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_debuginfo/

**Contents:**
- cilium-dbg debuginfo
- Options
- Options inherited from parent commands
- SEE ALSO

Request available debugging information from agent

---

## cilium-operator-alibabacloud completion zsh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_completion_zsh/

**Contents:**
- cilium-operator-alibabacloud completion zsh
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for zsh

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need to enable it. You can execute the following once:

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-alibabacloud completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bpf srv6 — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_srv6/

**Contents:**
- cilium-dbg bpf srv6
- Options
- Options inherited from parent commands
- SEE ALSO

Manage the SRv6 routing rules

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf srv6 policy - List SRv6 policy entries

cilium-dbg bpf srv6 sid - List SRv6 SID entries

cilium-dbg bpf srv6 vrf - List SRv6 VRF mappings

---

## Command Reference — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/

**Contents:**
- Command Reference

---

## cilium-dbg map events — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_map_events/

**Contents:**
- cilium-dbg map events
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Display cached list of events for a BPF map

cilium-dbg map - Access userspace cached content of BPF maps

---

## cilium-dbg policy delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_policy_delete/

**Contents:**
- cilium-dbg policy delete
- Options
- Options inherited from parent commands
- SEE ALSO

Delete policy rules (deprecated)

cilium-dbg policy - Manage security policies

---

## cilium-operator-alibabacloud hive dot-graph — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_hive_dot-graph/

**Contents:**
- cilium-operator-alibabacloud hive dot-graph
- Options
- Options inherited from parent commands
- SEE ALSO

Output the dependencies graph in graphviz dot format

cilium-operator-alibabacloud hive - Inspect the hive

---

## cilium-health ping — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_ping/

**Contents:**
- cilium-health ping
- Options
- Options inherited from parent commands
- SEE ALSO

Check whether the cilium-health API is up

cilium-health - Cilium Health Client

---

## cilium-dbg preflight migrate-identity — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_preflight_migrate-identity/

**Contents:**
- cilium-dbg preflight migrate-identity
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Migrate KVStore-backed identities to kubernetes CRD-backed identities

migrate-identity allows migrating to CRD-backed identities while minimizing connection interruptions. It will allocate a CRD-backed identity, with the same numeric security identity, for each cilium security identity defined in the kvstore. When cilium-agents are restarted with identity-allocation-mode set to CRD the numeric identities will then be equivalent between new instances and not-upgraded ones. In cases where the numeric identity is already in-use by a different set of labels, a new numeric identity is created.

cilium-dbg preflight - Cilium upgrade helper

---

## cilium-operator-azure troubleshoot clustermesh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_troubleshoot_clustermesh/

**Contents:**
- cilium-operator-azure troubleshoot clustermesh
- Options
- SEE ALSO

Troubleshoot connectivity towards remote clusters

cilium-operator-azure troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-operator-azure completion fish — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_completion_fish/

**Contents:**
- cilium-operator-azure completion fish
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for fish

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-azure completion - Generate the autocompletion script for the specified shell

---

## cilium-agent completion bash — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_completion_bash/

**Contents:**
- cilium-agent completion bash
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for bash

Generate the autocompletion script for the bash shell.

This script depends on the ‘bash-completion’ package. If it is not installed already, you can install it via your OS’s package manager.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-agent completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg envoy admin serverinfo — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_serverinfo/

**Contents:**
- cilium-dbg envoy admin serverinfo
- Options
- Options inherited from parent commands
- SEE ALSO

View server info of Envoy Proxy

cilium-dbg envoy admin - Access Envoy Admin Interface

---

## cilium-agent completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_completion/

**Contents:**
- cilium-agent completion
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for the specified shell

Generate the autocompletion script for cilium-agent for the specified shell. See each sub-command’s help for details on how to use the generated script.

cilium-agent - Run the cilium agent

cilium-agent completion bash - Generate the autocompletion script for bash

cilium-agent completion fish - Generate the autocompletion script for fish

cilium-agent completion powershell - Generate the autocompletion script for powershell

cilium-agent completion zsh - Generate the autocompletion script for zsh

---

## cilium-dbg bpf endpoint — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_endpoint/

**Contents:**
- cilium-dbg bpf endpoint
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf endpoint delete - Delete local endpoint entries

cilium-dbg bpf endpoint list - List local endpoint entries

---

## cilium-dbg bpf egress list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_egress_list/

**Contents:**
- cilium-dbg bpf egress list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List egress policy entries

List egress policy entries.

cilium-dbg bpf egress - Manage the egress routing rules

---

## cilium-dbg bpf policy add — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_policy_add/

**Contents:**
- cilium-dbg bpf policy add
- Options
- Options inherited from parent commands
- SEE ALSO

Add/update policy entry

cilium-dbg bpf policy - Manage policy related BPF maps

---

## cilium-operator-aws — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws/

**Contents:**
- cilium-operator-aws
- Options
- SEE ALSO

Run cilium-operator-aws

cilium-operator-aws completion - Generate the autocompletion script for the specified shell

cilium-operator-aws hive - Inspect the hive

cilium-operator-aws metrics - Access metric status of the operator

cilium-operator-aws shell - Connect to the Cilium shell

cilium-operator-aws status - Display status of operator

cilium-operator-aws troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-health — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health/

**Contents:**
- cilium-health
- Synopsis
- Options
- SEE ALSO

Client for querying the Cilium health status API

cilium-health completion - Generate the autocompletion script for the specified shell

cilium-health get - Display local cilium agent status

cilium-health ping - Check whether the cilium-health API is up

cilium-health status - Display cilium connectivity to other nodes

---

## cilium-operator-azure completion powershell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_completion_powershell/

**Contents:**
- cilium-operator-azure completion powershell
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for powershell

Generate the autocompletion script for powershell.

To load completions in your current shell session:

To load completions for every new session, add the output of the above command to your powershell profile.

cilium-operator-azure completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg map — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_map/

**Contents:**
- cilium-dbg map
- Options
- Options inherited from parent commands
- SEE ALSO

Access userspace cached content of BPF maps

cilium-dbg map events - Display cached list of events for a BPF map

cilium-dbg map get - Display cached content of given BPF map

cilium-dbg map list - List all open BPF maps

---

## cilium-dbg ip get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_ip_get/

**Contents:**
- cilium-dbg ip get
- Options
- Options inherited from parent commands
- SEE ALSO

Display IP Cache information

cilium-dbg ip - Manage IP addresses and associated information

---

## cilium-dbg bpf multicast group add — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_group_add/

**Contents:**
- cilium-dbg bpf multicast group add
- Synopsis
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Add a multicast group.

Add a multicast group to the node.

cilium-dbg bpf multicast group - Manage the multicast groups.

---

## cilium-dbg bpf ct flush — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ct_flush/

**Contents:**
- cilium-dbg bpf ct flush
- Options
- Options inherited from parent commands
- SEE ALSO

Flush all connection tracking entries

cilium-dbg bpf ct - Connection tracking tables

---

## cilium-dbg bpf nat retries flush — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nat_retries_flush/

**Contents:**
- cilium-dbg bpf nat retries flush
- Options
- Options inherited from parent commands
- SEE ALSO

Reset the NAT retries histogram

cilium-dbg bpf nat retries - Histogram of retries

---

## cilium-dbg node list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_node_list/

**Contents:**
- cilium-dbg node list
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg node - Manage cluster nodes

---

## cilium-dbg recorder update — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_recorder_update/

**Contents:**
- cilium-dbg recorder update
- Options
- Options inherited from parent commands
- SEE ALSO

Update individual pcap recorder

cilium-dbg recorder - Introspect or mangle pcap recorder

---

## cilium-health completion fish — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_completion_fish/

**Contents:**
- cilium-health completion fish
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Generate the autocompletion script for fish

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-health completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg envoy admin config — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_config/

**Contents:**
- cilium-dbg envoy admin config
- Options
- Options inherited from parent commands
- SEE ALSO

View config dump of Envoy Proxy

cilium-dbg envoy admin - Access Envoy Admin Interface

---

## cilium-health completion bash — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_completion_bash/

**Contents:**
- cilium-health completion bash
- Synopsis
  - Linux:
  - macOS:
- Options
- Options inherited from parent commands
- SEE ALSO

Generate the autocompletion script for bash

Generate the autocompletion script for the bash shell.

This script depends on the ‘bash-completion’ package. If it is not installed already, you can install it via your OS’s package manager.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-health completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg troubleshoot kvstore — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_troubleshoot_kvstore/

**Contents:**
- cilium-dbg troubleshoot kvstore
- Options
- Options inherited from parent commands
- SEE ALSO

Troubleshoot connectivity towards the etcd kvstore

cilium-dbg troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg bpf nodeid list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nodeid_list/

**Contents:**
- cilium-dbg bpf nodeid list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List node IDs and their IP addresses

List node IDs and their IP addresses.

cilium-dbg bpf nodeid - Manage the node IDs

---

## cilium-dbg policy — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_policy/

**Contents:**
- cilium-dbg policy
- Options
- Options inherited from parent commands
- SEE ALSO

Manage security policies

cilium-dbg policy delete - Delete policy rules (deprecated)

cilium-dbg policy get - Display policy node information (deprecated)

cilium-dbg policy import - Import security policy in JSON format (deprecated)

cilium-dbg policy selectors - Display cached information about selectors

cilium-dbg policy validate - Validate a policy (deprecated)

cilium-dbg policy wait - Wait for all endpoints to have updated to a given policy revision

---

## cilium-operator-alibabacloud completion fish — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_completion_fish/

**Contents:**
- cilium-operator-alibabacloud completion fish
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for fish

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-alibabacloud completion - Generate the autocompletion script for the specified shell

---

## cilium-operator-azure completion bash — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_completion_bash/

**Contents:**
- cilium-operator-azure completion bash
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for bash

Generate the autocompletion script for the bash shell.

This script depends on the ‘bash-completion’ package. If it is not installed already, you can install it via your OS’s package manager.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-azure completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bgp route-policies — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bgp_route-policies/

**Contents:**
- cilium-dbg bgp route-policies
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List configured route policies

List route policies configured in the underlying routing daemon

cilium-dbg bgp - Access to BGP control plane

---

## cilium-dbg bpf srv6 sid — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_srv6_sid/

**Contents:**
- cilium-dbg bpf srv6 sid
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List SRv6 SID entries

List SRv6 SID entries.

cilium-dbg bpf srv6 - Manage the SRv6 routing rules

---

## cilium-dbg bpf vtep — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_vtep/

**Contents:**
- cilium-dbg bpf vtep
- Options
- Options inherited from parent commands
- SEE ALSO

Manage the VTEP mappings for IP/CIDR <-> VTEP MAC/IP

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf vtep delete - Delete vtep entries

cilium-dbg bpf vtep list - List VTEP CIDR and their corresponding VTEP MAC/IP

cilium-dbg bpf vtep update - Update vtep entries

---

## cilium-dbg bpf fs show — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_fs_show/

**Contents:**
- cilium-dbg bpf fs show
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Show bpf filesystem mount details

cilium-dbg bpf fs - BPF filesystem mount

---

## cilium-dbg envoy admin logging set global — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_logging_set_global/

**Contents:**
- cilium-dbg envoy admin logging set global
- Options
- Options inherited from parent commands
- SEE ALSO

Change global logging level for all loggers of Envoy Proxy

cilium-dbg envoy admin logging set - Change logging levels of Envoy Proxy

---

## cilium-dbg bpf — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf/

**Contents:**
- cilium-dbg bpf
- Options
- Options inherited from parent commands
- SEE ALSO

Direct access to local BPF maps

cilium-dbg bpf auth - Manage authenticated connections between identities

cilium-dbg bpf bandwidth - BPF datapath bandwidth settings

cilium-dbg bpf config - Manage runtime config

cilium-dbg bpf ct - Connection tracking tables

cilium-dbg bpf egress - Manage the egress routing rules

cilium-dbg bpf endpoint - Local endpoint map

cilium-dbg bpf frag - Manage the IPv4 datagram fragments

cilium-dbg bpf fs - BPF filesystem mount

cilium-dbg bpf ipcache - Manage the IPCache mappings for IP/CIDR <-> Identity

cilium-dbg bpf ipmasq - ip-masq-agent CIDRs

cilium-dbg bpf lb - Load-balancing configuration

cilium-dbg bpf metrics - BPF datapath traffic metrics

cilium-dbg bpf multicast - Manage multicast BPF programs

cilium-dbg bpf nat - NAT mapping tables

cilium-dbg bpf nodeid - Manage the node IDs

cilium-dbg bpf policy - Manage policy related BPF maps

cilium-dbg bpf recorder - PCAP recorder

cilium-dbg bpf sha - Manage compiled BPF template objects

cilium-dbg bpf socknat - Socket NAT operations

cilium-dbg bpf srv6 - Manage the SRv6 routing rules

cilium-dbg bpf vtep - Manage the VTEP mappings for IP/CIDR <-> VTEP MAC/IP

---

## cilium-dbg encrypt flush — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_encrypt_flush/

**Contents:**
- cilium-dbg encrypt flush
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Flushes the current IPsec state

Will cause a short connectivity disruption

cilium-dbg encrypt - Manage transparent encryption

---

## cilium-dbg statedb — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_statedb/

**Contents:**
- cilium-dbg statedb
- Options
- Options inherited from parent commands
- SEE ALSO

Inspect StateDB (deprecated)

cilium-dbg statedb dump - Dump StateDB contents as JSON

---

## cilium-dbg bpf lb list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_lb_list/

**Contents:**
- cilium-dbg bpf lb list
- Options
- Options inherited from parent commands
- SEE ALSO

List load-balancing configuration

cilium-dbg bpf lb - Load-balancing configuration

---

## cilium-dbg bpf vtep delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_vtep_delete/

**Contents:**
- cilium-dbg bpf vtep delete
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Delete vtep entries using vtep CIDR.

cilium-dbg bpf vtep - Manage the VTEP mappings for IP/CIDR <-> VTEP MAC/IP

---

## cilium-operator — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/index_cilium-operator/

**Contents:**
- cilium-operator
- cilium-operator-alibabacloud
- cilium-operator-aws
- cilium-operator-azure
- cilium-operator-generic
- cilium-operator

---

## cilium-dbg metrics list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_metrics_list/

**Contents:**
- cilium-dbg metrics list
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg metrics - Access metric status

---

## cilium-operator-azure shell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_shell/

**Contents:**
- cilium-operator-azure shell
- Options
- SEE ALSO

Connect to the Cilium shell

cilium-operator-azure - Run cilium-operator-azure

---

## cilium-dbg kvstore get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_kvstore_get/

**Contents:**
- cilium-dbg kvstore get
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg kvstore - Direct access to the kvstore

---

## cilium-dbg troubleshoot clustermesh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_troubleshoot_clustermesh/

**Contents:**
- cilium-dbg troubleshoot clustermesh
- Options
- Options inherited from parent commands
- SEE ALSO

Troubleshoot connectivity towards remote clusters

cilium-dbg troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg map get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_map_get/

**Contents:**
- cilium-dbg map get
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Display cached content of given BPF map

cilium-dbg map - Access userspace cached content of BPF maps

---

## cilium-operator-azure status clustermesh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_status_clustermesh/

**Contents:**
- cilium-operator-azure status clustermesh
- Options
- SEE ALSO

Display status of remote clusters

cilium-operator-azure status - Display status of operator

---

## cilium-operator-aws troubleshoot — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_troubleshoot/

**Contents:**
- cilium-operator-aws troubleshoot
- Options
- SEE ALSO

Run troubleshooting utilities to check control-plane connectivity

cilium-operator-aws - Run cilium-operator-aws

cilium-operator-aws troubleshoot clustermesh - Troubleshoot connectivity towards remote clusters

cilium-operator-aws troubleshoot kvstore - Troubleshoot connectivity towards the etcd kvstore

---

## cilium-health completion zsh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_completion_zsh/

**Contents:**
- cilium-health completion zsh
- Synopsis
  - Linux:
  - macOS:
- Options
- Options inherited from parent commands
- SEE ALSO

Generate the autocompletion script for zsh

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need to enable it. You can execute the following once:

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-health completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bpf recorder — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_recorder/

**Contents:**
- cilium-dbg bpf recorder
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf recorder list - List PCAP recorder entries

---

## cilium-health completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_completion/

**Contents:**
- cilium-health completion
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Generate the autocompletion script for the specified shell

Generate the autocompletion script for cilium-health for the specified shell. See each sub-command’s help for details on how to use the generated script.

cilium-health - Cilium Health Client

cilium-health completion bash - Generate the autocompletion script for bash

cilium-health completion fish - Generate the autocompletion script for fish

cilium-health completion powershell - Generate the autocompletion script for powershell

cilium-health completion zsh - Generate the autocompletion script for zsh

---

## cilium-dbg bpf recorder list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_recorder_list/

**Contents:**
- cilium-dbg bpf recorder list
- Options
- Options inherited from parent commands
- SEE ALSO

List PCAP recorder entries

cilium-dbg bpf recorder - PCAP recorder

---

## cilium-operator-aws status — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_status/

**Contents:**
- cilium-operator-aws status
- Options
- SEE ALSO

Display status of operator

cilium-operator-aws - Run cilium-operator-aws

cilium-operator-aws status clustermesh - Display status of remote clusters

---

## cilium-dbg kvstore set — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_kvstore_set/

**Contents:**
- cilium-dbg kvstore set
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg kvstore - Direct access to the kvstore

---

## cilium-operator-alibabacloud troubleshoot — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_troubleshoot/

**Contents:**
- cilium-operator-alibabacloud troubleshoot
- Options
- SEE ALSO

Run troubleshooting utilities to check control-plane connectivity

cilium-operator-alibabacloud - Run cilium-operator-alibabacloud

cilium-operator-alibabacloud troubleshoot clustermesh - Troubleshoot connectivity towards remote clusters

cilium-operator-alibabacloud troubleshoot kvstore - Troubleshoot connectivity towards the etcd kvstore

---

## cilium-dbg bpf vtep update — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_vtep_update/

**Contents:**
- cilium-dbg bpf vtep update
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Create/Update vtep entry.

cilium-dbg bpf vtep - Manage the VTEP mappings for IP/CIDR <-> VTEP MAC/IP

---

## cilium-agent completion fish — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_completion_fish/

**Contents:**
- cilium-agent completion fish
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for fish

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-agent completion - Generate the autocompletion script for the specified shell

---

## cilium-agent — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/index_cilium-agent/

**Contents:**
- cilium-agent

---

## cilium-operator-alibabacloud completion powershell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_completion_powershell/

**Contents:**
- cilium-operator-alibabacloud completion powershell
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for powershell

Generate the autocompletion script for powershell.

To load completions in your current shell session:

To load completions for every new session, add the output of the above command to your powershell profile.

cilium-operator-alibabacloud completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bpf frag — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_frag/

**Contents:**
- cilium-dbg bpf frag
- Options
- Options inherited from parent commands
- SEE ALSO

Manage the IPv4 datagram fragments

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf frag list - List IPv4 and IPv6 fragments

---

## cilium-dbg endpoint disconnect — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint_disconnect/

**Contents:**
- cilium-dbg endpoint disconnect
- Options
- Options inherited from parent commands
- SEE ALSO

Disconnect an endpoint from the network

cilium-dbg endpoint - Manage endpoints

---

## cilium-dbg bpf nodeid — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nodeid/

**Contents:**
- cilium-dbg bpf nodeid
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf nodeid list - List node IDs and their IP addresses

---

## cilium-dbg policy selectors — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_policy_selectors/

**Contents:**
- cilium-dbg policy selectors
- Options
- Options inherited from parent commands
- SEE ALSO

Display cached information about selectors

cilium-dbg policy - Manage security policies

---

## cilium-dbg bpf endpoint delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_endpoint_delete/

**Contents:**
- cilium-dbg bpf endpoint delete
- Options
- Options inherited from parent commands
- SEE ALSO

Delete local endpoint entries

cilium-dbg bpf endpoint - Local endpoint map

---

## cilium-dbg bpf sha list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_sha_list/

**Contents:**
- cilium-dbg bpf sha list
- Options
- Options inherited from parent commands
- SEE ALSO

List BPF template objects.

cilium-dbg bpf sha - Manage compiled BPF template objects

---

## cilium-dbg policy validate — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_policy_validate/

**Contents:**
- cilium-dbg policy validate
- Options
- Options inherited from parent commands
- SEE ALSO

Validate a policy (deprecated)

cilium-dbg policy - Manage security policies

---

## cilium-operator-generic completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-generic_completion/

**Contents:**
- cilium-operator-generic completion
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for the specified shell

Generate the autocompletion script for cilium-operator-generic for the specified shell. See each sub-command’s help for details on how to use the generated script.

cilium-operator-generic - Run cilium-operator-generic

cilium-operator-generic completion bash - Generate the autocompletion script for bash

cilium-operator-generic completion fish - Generate the autocompletion script for fish

cilium-operator-generic completion powershell - Generate the autocompletion script for powershell

cilium-operator-generic completion zsh - Generate the autocompletion script for zsh

---

## cilium-dbg fqdn — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_fqdn/

**Contents:**
- cilium-dbg fqdn
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg fqdn cache - Manage fqdn proxy cache

cilium-dbg fqdn names - Show internal state Cilium has for DNS names / regexes

---

## cilium-dbg envoy admin — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin/

**Contents:**
- cilium-dbg envoy admin
- Options
- Options inherited from parent commands
- SEE ALSO

Access Envoy Admin Interface

cilium-dbg envoy - Manage Envoy Proxy

cilium-dbg envoy admin certs - List configured TLS certificates of Envoy Proxy

cilium-dbg envoy admin clusters - List configured clusters of Envoy Proxy

cilium-dbg envoy admin config - View config dump of Envoy Proxy

cilium-dbg envoy admin listeners - List configured listeners of Envoy Proxy

cilium-dbg envoy admin logging - List and change logging levels of Envoy Proxy

cilium-dbg envoy admin metrics - List Prometheus statistics of Envoy Proxy

cilium-dbg envoy admin serverinfo - View server info of Envoy Proxy

---

## cilium-dbg bpf policy delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_policy_delete/

**Contents:**
- cilium-dbg bpf policy delete
- Options
- Options inherited from parent commands
- SEE ALSO

Delete a policy entry

cilium-dbg bpf policy - Manage policy related BPF maps

---

## cilium-dbg bpf endpoint list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_endpoint_list/

**Contents:**
- cilium-dbg bpf endpoint list
- Options
- Options inherited from parent commands
- SEE ALSO

List local endpoint entries

cilium-dbg bpf endpoint - Local endpoint map

---

## cilium-operator-azure hive — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_hive/

**Contents:**
- cilium-operator-azure hive
- Options
- SEE ALSO

cilium-operator-azure - Run cilium-operator-azure

cilium-operator-azure hive dot-graph - Output the dependencies graph in graphviz dot format

---

## cilium-dbg bpf socknat list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_socknat_list/

**Contents:**
- cilium-dbg bpf socknat list
- Options
- Options inherited from parent commands
- SEE ALSO

List socket-LB reverse NAT entries

cilium-dbg bpf socknat - Socket NAT operations

---

## cilium-operator-azure troubleshoot kvstore — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_troubleshoot_kvstore/

**Contents:**
- cilium-operator-azure troubleshoot kvstore
- Options
- SEE ALSO

Troubleshoot connectivity towards the etcd kvstore

cilium-operator-azure troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-agent shell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_shell/

**Contents:**
- cilium-agent shell
- Options
- SEE ALSO

Connect to the Cilium shell

cilium-agent - Run the cilium agent

---

## cilium-dbg config get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_config_get/

**Contents:**
- cilium-dbg config get
- Options
- Options inherited from parent commands
- SEE ALSO

Retrieve cilium configuration

cilium-dbg config - Cilium configuration options

---

## cilium-dbg map list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_map_list/

**Contents:**
- cilium-dbg map list
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

List all open BPF maps

cilium-dbg map - Access userspace cached content of BPF maps

---

## cilium-agent completion zsh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_completion_zsh/

**Contents:**
- cilium-agent completion zsh
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for zsh

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need to enable it. You can execute the following once:

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-agent completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bpf nat retries list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nat_retries_list/

**Contents:**
- cilium-dbg bpf nat retries list
- Options
- Options inherited from parent commands
- SEE ALSO

Show the NAT retries histogram

cilium-dbg bpf nat retries - Histogram of retries

---

## cilium-dbg fqdn cache — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_fqdn_cache/

**Contents:**
- cilium-dbg fqdn cache
- Options
- Options inherited from parent commands
- SEE ALSO

Manage fqdn proxy cache

cilium-dbg fqdn - Manage fqdn proxy

cilium-dbg fqdn cache clean - Clean fqdn cache

cilium-dbg fqdn cache list - List fqdn cache contents

---

## cilium-dbg monitor — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_monitor/

**Contents:**
- cilium-dbg monitor
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Display BPF program events

The monitor displays notifications and events emitted by the BPF programs attached to endpoints and devices. This includes:

Dropped packet notifications

Captured packet traces

Policy verdict notifications

Debugging information

---

## cilium-dbg bpf ipcache list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipcache_list/

**Contents:**
- cilium-dbg bpf ipcache list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List endpoint IPs (local and remote) and their corresponding security identities

List endpoint IPs (local and remote) and their corresponding security identities.

cilium-dbg bpf ipcache - Manage the IPCache mappings for IP/CIDR <-> Identity

---

## cilium-dbg ip list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_ip_list/

**Contents:**
- cilium-dbg ip list
- Options
- Options inherited from parent commands
- SEE ALSO

List IP addresses in the userspace IPcache

cilium-dbg ip - Manage IP addresses and associated information

---

## cilium-dbg envoy admin clusters — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_clusters/

**Contents:**
- cilium-dbg envoy admin clusters
- Options
- Options inherited from parent commands
- SEE ALSO

List configured clusters of Envoy Proxy

cilium-dbg envoy admin - Access Envoy Admin Interface

---

## cilium-dbg kvstore — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_kvstore/

**Contents:**
- cilium-dbg kvstore
- Options
- Options inherited from parent commands
- SEE ALSO

Direct access to the kvstore

cilium-dbg kvstore delete - Delete a key

cilium-dbg kvstore get - Retrieve a key

cilium-dbg kvstore set - Set a key and value

---

## cilium-dbg prefilter — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_prefilter/

**Contents:**
- cilium-dbg prefilter
- Options
- Options inherited from parent commands
- SEE ALSO

Manage XDP CIDR filters

cilium-dbg prefilter delete - Delete CIDR filters

cilium-dbg prefilter list - List CIDR filters

cilium-dbg prefilter update - Update CIDR filters

---

## cilium-dbg bpf nat — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nat/

**Contents:**
- cilium-dbg bpf nat
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf nat flush - Flush all NAT mapping entries

cilium-dbg bpf nat list - List all NAT mapping entries

cilium-dbg bpf nat retries - Histogram of retries

---

## cilium-operator-alibabacloud shell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_shell/

**Contents:**
- cilium-operator-alibabacloud shell
- Options
- SEE ALSO

Connect to the Cilium shell

cilium-operator-alibabacloud - Run cilium-operator-alibabacloud

---

## cilium-bugtool — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/index_cilium-bugtool/

**Contents:**
- cilium-bugtool

---

## cilium-dbg identity get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_identity_get/

**Contents:**
- cilium-dbg identity get
- Options
- Options inherited from parent commands
- SEE ALSO

Retrieve information about an identity

cilium-dbg identity - Manage security identities

---

## cilium-operator-azure troubleshoot — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_troubleshoot/

**Contents:**
- cilium-operator-azure troubleshoot
- Options
- SEE ALSO

Run troubleshooting utilities to check control-plane connectivity

cilium-operator-azure - Run cilium-operator-azure

cilium-operator-azure troubleshoot clustermesh - Troubleshoot connectivity towards remote clusters

cilium-operator-azure troubleshoot kvstore - Troubleshoot connectivity towards the etcd kvstore

---

## cilium-agent hive dot-graph — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_hive_dot-graph/

**Contents:**
- cilium-agent hive dot-graph
- Options
- Options inherited from parent commands
- SEE ALSO

Output the dependencies graph in graphviz dot format

cilium-agent hive - Inspect the hive

---

## cilium-operator-azure metrics — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_metrics/

**Contents:**
- cilium-operator-azure metrics
- Options
- SEE ALSO

Access metric status of the operator

cilium-operator-azure - Run cilium-operator-azure

cilium-operator-azure metrics list - List all metrics for the operator

---

## cilium-dbg ip — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_ip/

**Contents:**
- cilium-dbg ip
- Options
- Options inherited from parent commands
- SEE ALSO

Manage IP addresses and associated information

cilium-dbg ip get - Display IP Cache information

cilium-dbg ip list - List IP addresses in the userspace IPcache

---

## cilium-dbg cgroups — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_cgroups/

**Contents:**
- cilium-dbg cgroups
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg cgroups list - Display cgroup metadata maintained by Cilium

---

## cilium-bugtool completion fish — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-bugtool_completion_fish/

**Contents:**
- cilium-bugtool completion fish
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for fish

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-bugtool completion - Generate the autocompletion script for the specified shell

---

## cilium-operator-azure completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_completion/

**Contents:**
- cilium-operator-azure completion
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for the specified shell

Generate the autocompletion script for cilium-operator-azure for the specified shell. See each sub-command’s help for details on how to use the generated script.

cilium-operator-azure - Run cilium-operator-azure

cilium-operator-azure completion bash - Generate the autocompletion script for bash

cilium-operator-azure completion fish - Generate the autocompletion script for fish

cilium-operator-azure completion powershell - Generate the autocompletion script for powershell

cilium-operator-azure completion zsh - Generate the autocompletion script for zsh

---

## cilium-operator-aws hive dot-graph — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_hive_dot-graph/

**Contents:**
- cilium-operator-aws hive dot-graph
- Options
- Options inherited from parent commands
- SEE ALSO

Output the dependencies graph in graphviz dot format

cilium-operator-aws hive - Inspect the hive

---

## cilium-dbg bpf multicast — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast/

**Contents:**
- cilium-dbg bpf multicast
- Options
- Options inherited from parent commands
- SEE ALSO

Manage multicast BPF programs

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf multicast group - Manage the multicast groups.

cilium-dbg bpf multicast subscriber - Manage the multicast subscribers.

---

## cilium-dbg bpf multicast subscriber add — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_subscriber_add/

**Contents:**
- cilium-dbg bpf multicast subscriber add
- Synopsis
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Add a remote subscriber to the multicast group.

Add a remote subscriber to the multicast group. Only remote subscribers are added via command line. Local subscribers will be automatically populated in the map based on IGMP messages. Remote subscribers are typically other Cilium nodes, identified by internal IP of the node. To add remote subscriber, following information is required:

group: multicast group address to which subscriber is added.

subscriber-address: subscriber IP address.

cilium-dbg bpf multicast subscriber - Manage the multicast subscribers.

---

## cilium-dbg bpf sha — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_sha/

**Contents:**
- cilium-dbg bpf sha
- Options
- Options inherited from parent commands
- SEE ALSO

Manage compiled BPF template objects

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf sha get - Get datapath SHA header

cilium-dbg bpf sha list - List BPF template objects.

---

## cilium-dbg lrp — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_lrp/

**Contents:**
- cilium-dbg lrp
- Options
- Options inherited from parent commands
- SEE ALSO

Manage local redirect policies

cilium-dbg lrp list - List local redirect policies

---

## cilium-dbg bpf ipcache get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipcache_get/

**Contents:**
- cilium-dbg bpf ipcache get
- Options
- Options inherited from parent commands
- SEE ALSO

Retrieve identity for an ip

cilium-dbg bpf ipcache - Manage the IPCache mappings for IP/CIDR <-> Identity

---

## cilium-health status — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_status/

**Contents:**
- cilium-health status
- Options
- Options inherited from parent commands
- SEE ALSO

Display cilium connectivity to other nodes

cilium-health - Cilium Health Client

---

## cilium-bugtool — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-bugtool/

**Contents:**
- cilium-bugtool
- Examples
- Options
- SEE ALSO

Collects agent & system information useful for bug reporting

cilium-bugtool completion - Generate the autocompletion script for the specified shell

---

## cilium-operator-aws troubleshoot clustermesh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_troubleshoot_clustermesh/

**Contents:**
- cilium-operator-aws troubleshoot clustermesh
- Options
- SEE ALSO

Troubleshoot connectivity towards remote clusters

cilium-operator-aws troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg recorder list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_recorder_list/

**Contents:**
- cilium-dbg recorder list
- Options
- Options inherited from parent commands
- SEE ALSO

List current pcap recorders

cilium-dbg recorder - Introspect or mangle pcap recorder

---

## cilium-dbg envoy admin certs — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_certs/

**Contents:**
- cilium-dbg envoy admin certs
- Options
- Options inherited from parent commands
- SEE ALSO

List configured TLS certificates of Envoy Proxy

cilium-dbg envoy admin - Access Envoy Admin Interface

---

## cilium-dbg bpf multicast group delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_group_delete/

**Contents:**
- cilium-dbg bpf multicast group delete
- Synopsis
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Delete a multicast group.

Delete a multicast group from the node.

cilium-dbg bpf multicast group - Manage the multicast groups.

---

## cilium-dbg bgp peers — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bgp_peers/

**Contents:**
- cilium-dbg bgp peers
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List current state of all peers

List state of all peers defined in CiliumBGPPeeringPolicy

cilium-dbg bgp - Access to BGP control plane

---

## cilium-operator-azure hive dot-graph — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_hive_dot-graph/

**Contents:**
- cilium-operator-azure hive dot-graph
- Options
- Options inherited from parent commands
- SEE ALSO

Output the dependencies graph in graphviz dot format

cilium-operator-azure hive - Inspect the hive

---

## cilium-operator-alibabacloud — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud/

**Contents:**
- cilium-operator-alibabacloud
- Options
- SEE ALSO

Run cilium-operator-alibabacloud

cilium-operator-alibabacloud completion - Generate the autocompletion script for the specified shell

cilium-operator-alibabacloud hive - Inspect the hive

cilium-operator-alibabacloud metrics - Access metric status of the operator

cilium-operator-alibabacloud shell - Connect to the Cilium shell

cilium-operator-alibabacloud status - Display status of operator

cilium-operator-alibabacloud troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg bgp routes — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bgp_routes/

**Contents:**
- cilium-dbg bgp routes
- Synopsis
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

List routes in the BGP Control Plane’s RIBs

List routes in the BGP Control Plane’s Routing Information Bases (RIBs)

cilium-dbg bgp - Access to BGP control plane

---

## cilium-dbg bpf auth flush — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_auth_flush/

**Contents:**
- cilium-dbg bpf auth flush
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Deletes all entries for authenticated connections between identities

Deletes all entries for authenticated connections between identities

cilium-dbg bpf auth - Manage authenticated connections between identities

---

## cilium-dbg kvstore delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_kvstore_delete/

**Contents:**
- cilium-dbg kvstore delete
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg kvstore - Direct access to the kvstore

---

## cilium-dbg bpf ct — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ct/

**Contents:**
- cilium-dbg bpf ct
- Options
- Options inherited from parent commands
- SEE ALSO

Connection tracking tables

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf ct flush - Flush all connection tracking entries

cilium-dbg bpf ct list - List connection tracking entries

---

## cilium-dbg bpf metrics — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_metrics/

**Contents:**
- cilium-dbg bpf metrics
- Options
- Options inherited from parent commands
- SEE ALSO

BPF datapath traffic metrics

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf metrics list - List BPF datapath traffic metrics

---

## cilium-dbg bpf config — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_config/

**Contents:**
- cilium-dbg bpf config
- Options
- Options inherited from parent commands
- SEE ALSO

Manage runtime config

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf config list - List all runtime config entries

---

## cilium-dbg nodeid — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_nodeid/

**Contents:**
- cilium-dbg nodeid
- Options
- Options inherited from parent commands
- SEE ALSO

List node IDs and associated information

cilium-dbg nodeid list - List node IDs and the associated IP addresses

---

## cilium-dbg identity list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_identity_list/

**Contents:**
- cilium-dbg identity list
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg identity - Manage security identities

---

## cilium-health get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_get/

**Contents:**
- cilium-health get
- Options
- Options inherited from parent commands
- SEE ALSO

Display local cilium agent status

cilium-health - Cilium Health Client

---

## cilium-operator-aws metrics list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_metrics_list/

**Contents:**
- cilium-operator-aws metrics list
- Options
- SEE ALSO

List all metrics for the operator

cilium-operator-aws metrics - Access metric status of the operator

---

## cilium-dbg sysdump — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_sysdump/

**Contents:**
- cilium-dbg sysdump
- Options
- Options inherited from parent commands
- SEE ALSO

Provide instructions on dumping cluster-wide system state

---

## cilium-dbg envoy admin logging set loggers — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_logging_set_loggers/

**Contents:**
- cilium-dbg envoy admin logging set loggers
- Options
- Options inherited from parent commands
- SEE ALSO

Change logging level of a list of loggers of Envoy Proxy

cilium-dbg envoy admin logging set - Change logging levels of Envoy Proxy

---

## cilium-dbg statedb dump — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_statedb_dump/

**Contents:**
- cilium-dbg statedb dump
- Options
- Options inherited from parent commands
- SEE ALSO

Dump StateDB contents as JSON

cilium-dbg statedb - Inspect StateDB (deprecated)

---

## cilium-operator-alibabacloud status clustermesh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_status_clustermesh/

**Contents:**
- cilium-operator-alibabacloud status clustermesh
- Options
- SEE ALSO

Display status of remote clusters

cilium-operator-alibabacloud status - Display status of operator

---

## cilium-dbg envoy — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy/

**Contents:**
- cilium-dbg envoy
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg envoy admin - Access Envoy Admin Interface

---

## cilium-health completion powershell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-health_completion_powershell/

**Contents:**
- cilium-health completion powershell
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Generate the autocompletion script for powershell

Generate the autocompletion script for powershell.

To load completions in your current shell session:

To load completions for every new session, add the output of the above command to your powershell profile.

cilium-health completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg envoy admin metrics — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_envoy_admin_metrics/

**Contents:**
- cilium-dbg envoy admin metrics
- Options
- Options inherited from parent commands
- SEE ALSO

List Prometheus statistics of Envoy Proxy

cilium-dbg envoy admin - Access Envoy Admin Interface

---

## cilium-dbg bpf auth — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_auth/

**Contents:**
- cilium-dbg bpf auth
- Options
- Options inherited from parent commands
- SEE ALSO

Manage authenticated connections between identities

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf auth flush - Deletes all entries for authenticated connections between identities

cilium-dbg bpf auth list - List all authenticated connections between identities

---

## cilium-agent completion powershell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent_completion_powershell/

**Contents:**
- cilium-agent completion powershell
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for powershell

Generate the autocompletion script for powershell.

To load completions in your current shell session:

To load completions for every new session, add the output of the above command to your powershell profile.

cilium-agent completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg endpoint config — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint_config/

**Contents:**
- cilium-dbg endpoint config
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

View & modify endpoint configuration

cilium-dbg endpoint - Manage endpoints

---

## cilium-dbg bpf ipmasq — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipmasq/

**Contents:**
- cilium-dbg bpf ipmasq
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf ipmasq list - List ip-masq-agent CIDRs

---

## cilium-dbg policy import — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_policy_import/

**Contents:**
- cilium-dbg policy import
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Import security policy in JSON format (deprecated)

cilium-dbg policy - Manage security policies

---

## cilium-dbg endpoint — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint/

**Contents:**
- cilium-dbg endpoint
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg endpoint config - View & modify endpoint configuration

cilium-dbg endpoint disconnect - Disconnect an endpoint from the network

cilium-dbg endpoint get - Display endpoint information

cilium-dbg endpoint health - View endpoint health

cilium-dbg endpoint labels - Manage label configuration of endpoint

cilium-dbg endpoint list - List all endpoints

cilium-dbg endpoint log - View endpoint status log

---

## cilium-operator-alibabacloud metrics — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_metrics/

**Contents:**
- cilium-operator-alibabacloud metrics
- Options
- SEE ALSO

Access metric status of the operator

cilium-operator-alibabacloud - Run cilium-operator-alibabacloud

cilium-operator-alibabacloud metrics list - List all metrics for the operator

---

## cilium-dbg encrypt status — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_encrypt_status/

**Contents:**
- cilium-dbg encrypt status
- Options
- Options inherited from parent commands
- SEE ALSO

Display the current encryption state

cilium-dbg encrypt - Manage transparent encryption

---

## cilium-dbg bpf multicast subscriber delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_subscriber_delete/

**Contents:**
- cilium-dbg bpf multicast subscriber delete
- Synopsis
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Delete a subscriber from the multicast group.

Delete a subscriber from the given multicast group. To delete remote subscriber, following information is required:

group: multicast group address from which subscriber is deleted.

subscriber-address: subscriber IP address

cilium-dbg bpf multicast subscriber - Manage the multicast subscribers.

---

## cilium-dbg bpf multicast subscriber list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_subscriber_list/

**Contents:**
- cilium-dbg bpf multicast subscriber list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List the multicast subscribers for the given group.

List the multicast subscribers for the given group. To get the subscribers for all the groups, use ‘cilium-dbg bpf multicast subscriber list all’.

cilium-dbg bpf multicast subscriber - Manage the multicast subscribers.

---

## cilium-operator-alibabacloud troubleshoot clustermesh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_troubleshoot_clustermesh/

**Contents:**
- cilium-operator-alibabacloud troubleshoot clustermesh
- Options
- SEE ALSO

Troubleshoot connectivity towards remote clusters

cilium-operator-alibabacloud troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg bpf nat list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nat_list/

**Contents:**
- cilium-dbg bpf nat list
- Options
- Options inherited from parent commands
- SEE ALSO

List all NAT mapping entries

cilium-dbg bpf nat - NAT mapping tables

---

## cilium-dbg — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg/

**Contents:**
- cilium-dbg
- Synopsis
- Options
- SEE ALSO

CLI for interacting with the local Cilium Agent

cilium-dbg bgp - Access to BGP control plane

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg build-config - Resolve all of the configuration sources that apply to this node

cilium-dbg cgroups - Cgroup metadata

cilium-dbg completion - Output shell completion code

cilium-dbg config - Cilium configuration options

cilium-dbg debuginfo - Request available debugging information from agent

cilium-dbg encrypt - Manage transparent encryption

cilium-dbg endpoint - Manage endpoints

cilium-dbg envoy - Manage Envoy Proxy

cilium-dbg fqdn - Manage fqdn proxy

cilium-dbg identity - Manage security identities

cilium-dbg ip - Manage IP addresses and associated information

cilium-dbg kvstore - Direct access to the kvstore

cilium-dbg loadinfo - Show load information

cilium-dbg lrp - Manage local redirect policies

cilium-dbg map - Access userspace cached content of BPF maps

cilium-dbg metrics - Access metric status

cilium-dbg monitor - Display BPF program events

cilium-dbg node - Manage cluster nodes

cilium-dbg nodeid - List node IDs and associated information

cilium-dbg policy - Manage security policies

cilium-dbg post-uninstall-cleanup - Remove system state installed by Cilium at runtime

cilium-dbg prefilter - Manage XDP CIDR filters

cilium-dbg preflight - Cilium upgrade helper

cilium-dbg recorder - Introspect or mangle pcap recorder

cilium-dbg service - List services & loadbalancers

cilium-dbg shell - Connect to the Cilium shell

cilium-dbg statedb - Inspect StateDB (deprecated)

cilium-dbg status - Display status of daemon

cilium-dbg sysdump - Provide instructions on dumping cluster-wide system state

cilium-dbg troubleshoot - Run troubleshooting utilities to check control-plane connectivity

cilium-dbg version - Print version information

---

## cilium-dbg bpf lb maglev — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_lb_maglev/

**Contents:**
- cilium-dbg bpf lb maglev
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf lb - Load-balancing configuration

cilium-dbg bpf lb maglev list - List Maglev lookup tables

---

## cilium-dbg troubleshoot — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_troubleshoot/

**Contents:**
- cilium-dbg troubleshoot
- Options
- Options inherited from parent commands
- SEE ALSO

Run troubleshooting utilities to check control-plane connectivity

cilium-dbg troubleshoot clustermesh - Troubleshoot connectivity towards remote clusters

cilium-dbg troubleshoot kvstore - Troubleshoot connectivity towards the etcd kvstore

---

## Key-Value Store — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/kvstore/

**Contents:**
- Key-Value Store
- etcd

Key Value Store Type: (etcd)

When using etcd, one of the following options need to be provided to configure the etcd endpoints:

Address of etcd endpoint

Path to an etcd configuration file.

Example of the etcd configuration file:

---

## cilium-operator-generic — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-generic/

**Contents:**
- cilium-operator-generic
- Options
- SEE ALSO

Run cilium-operator-generic

cilium-operator-generic completion - Generate the autocompletion script for the specified shell

cilium-operator-generic hive - Inspect the hive

cilium-operator-generic metrics - Access metric status of the operator

cilium-operator-generic shell - Connect to the Cilium shell

cilium-operator-generic status - Display status of operator

cilium-operator-generic troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg bpf nat flush — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nat_flush/

**Contents:**
- cilium-dbg bpf nat flush
- Options
- Options inherited from parent commands
- SEE ALSO

Flush all NAT mapping entries

cilium-dbg bpf nat - NAT mapping tables

---

## cilium-operator-aws completion fish — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_completion_fish/

**Contents:**
- cilium-operator-aws completion fish
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for fish

Generate the autocompletion script for the fish shell.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-aws completion - Generate the autocompletion script for the specified shell

---

## cilium-operator-aws completion powershell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_completion_powershell/

**Contents:**
- cilium-operator-aws completion powershell
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for powershell

Generate the autocompletion script for powershell.

To load completions in your current shell session:

To load completions for every new session, add the output of the above command to your powershell profile.

cilium-operator-aws completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bpf ct list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ct_list/

**Contents:**
- cilium-dbg bpf ct list
- Options
- Options inherited from parent commands
- SEE ALSO

List connection tracking entries

cilium-dbg bpf ct - Connection tracking tables

---

## cilium-dbg bpf multicast subscriber — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_subscriber/

**Contents:**
- cilium-dbg bpf multicast subscriber
- Options
- Options inherited from parent commands
- SEE ALSO

Manage the multicast subscribers.

cilium-dbg bpf multicast - Manage multicast BPF programs

cilium-dbg bpf multicast subscriber add - Add a remote subscriber to the multicast group.

cilium-dbg bpf multicast subscriber delete - Delete a subscriber from the multicast group.

cilium-dbg bpf multicast subscriber list - List the multicast subscribers for the given group.

---

## cilium-bugtool completion zsh — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-bugtool_completion_zsh/

**Contents:**
- cilium-bugtool completion zsh
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for zsh

Generate the autocompletion script for the zsh shell.

If shell completion is not already enabled in your environment you will need to enable it. You can execute the following once:

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-bugtool completion - Generate the autocompletion script for the specified shell

---

## cilium-dbg bpf lb maglev list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_lb_maglev_list/

**Contents:**
- cilium-dbg bpf lb maglev list
- Options
- Options inherited from parent commands
- SEE ALSO

List Maglev lookup tables

cilium-dbg bpf lb maglev - Maglev lookup table

---

## cilium-dbg bpf socknat — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_socknat/

**Contents:**
- cilium-dbg bpf socknat
- Options
- Options inherited from parent commands
- SEE ALSO

Socket NAT operations

cilium-dbg bpf - Direct access to local BPF maps

cilium-dbg bpf socknat list - List socket-LB reverse NAT entries

---

## cilium-agent — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-agent/

**Contents:**
- cilium-agent
- Options
- SEE ALSO

cilium-agent completion - Generate the autocompletion script for the specified shell

cilium-agent hive - Inspect the hive

cilium-agent shell - Connect to the Cilium shell

---

## cilium-dbg cgroups list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_cgroups_list/

**Contents:**
- cilium-dbg cgroups list
- Options
- Options inherited from parent commands
- SEE ALSO

Display cgroup metadata maintained by Cilium

cilium-dbg cgroups - Cgroup metadata

---

## cilium-operator — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator/

**Contents:**
- cilium-operator
- Options
- SEE ALSO

cilium-operator completion - Generate the autocompletion script for the specified shell

cilium-operator hive - Inspect the hive

cilium-operator metrics - Access metric status of the operator

cilium-operator shell - Connect to the Cilium shell

cilium-operator status - Display status of operator

cilium-operator troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg bpf frag list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_frag_list/

**Contents:**
- cilium-dbg bpf frag list
- Options
- Options inherited from parent commands
- SEE ALSO

List IPv4 and IPv6 fragments

cilium-dbg bpf frag - Manage the IPv4 datagram fragments

---

## cilium-dbg nodeid list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_nodeid_list/

**Contents:**
- cilium-dbg nodeid list
- Options
- Options inherited from parent commands
- SEE ALSO

List node IDs and the associated IP addresses

cilium-dbg nodeid - List node IDs and associated information

---

## cilium-dbg bpf multicast group list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_group_list/

**Contents:**
- cilium-dbg bpf multicast group list
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

List the multicast groups.

List the multicast groups configured on the node.

cilium-dbg bpf multicast group - Manage the multicast groups.

---

## cilium-operator-aws shell — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_shell/

**Contents:**
- cilium-operator-aws shell
- Options
- SEE ALSO

Connect to the Cilium shell

cilium-operator-aws - Run cilium-operator-aws

---

## cilium-dbg bpf sha get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_sha_get/

**Contents:**
- cilium-dbg bpf sha get
- Options
- Options inherited from parent commands
- SEE ALSO

Get datapath SHA header

cilium-dbg bpf sha - Manage compiled BPF template objects

---

## cilium-operator-aws hive — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_hive/

**Contents:**
- cilium-operator-aws hive
- Options
- SEE ALSO

cilium-operator-aws - Run cilium-operator-aws

cilium-operator-aws hive dot-graph - Output the dependencies graph in graphviz dot format

---

## cilium-dbg preflight validate-cnp — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_preflight_validate-cnp/

**Contents:**
- cilium-dbg preflight validate-cnp
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Validate Cilium Network Policies deployed in the cluster

Before upgrading Cilium it is recommended to run this validation checker to make sure the policies deployed are valid. The validator will verify if all policies deployed in the cluster are valid, in case they are not, an error is printed and the has an exit code 1 is returned.

cilium-dbg preflight - Cilium upgrade helper

---

## cilium-dbg loadinfo — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_loadinfo/

**Contents:**
- cilium-dbg loadinfo
- Options
- Options inherited from parent commands
- SEE ALSO

Show load information

---

## cilium-dbg bpf ipcache delete — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipcache_delete/

**Contents:**
- cilium-dbg bpf ipcache delete
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Delete an entry for a prefix

cilium-dbg bpf ipcache - Manage the IPCache mappings for IP/CIDR <-> Identity

---

## cilium-dbg bpf ipcache update — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_ipcache_update/

**Contents:**
- cilium-dbg bpf ipcache update
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Update an entry for a prefix

cilium-dbg bpf ipcache - Manage the IPCache mappings for IP/CIDR <-> Identity

---

## cilium-bugtool completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-bugtool_completion/

**Contents:**
- cilium-bugtool completion
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for the specified shell

Generate the autocompletion script for cilium-bugtool for the specified shell. See each sub-command’s help for details on how to use the generated script.

cilium-bugtool - Collects agent & system information useful for bug reporting

cilium-bugtool completion bash - Generate the autocompletion script for bash

cilium-bugtool completion fish - Generate the autocompletion script for fish

cilium-bugtool completion powershell - Generate the autocompletion script for powershell

cilium-bugtool completion zsh - Generate the autocompletion script for zsh

---

## cilium-operator-alibabacloud troubleshoot kvstore — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_troubleshoot_kvstore/

**Contents:**
- cilium-operator-alibabacloud troubleshoot kvstore
- Options
- SEE ALSO

Troubleshoot connectivity towards the etcd kvstore

cilium-operator-alibabacloud troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg fqdn cache list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_fqdn_cache_list/

**Contents:**
- cilium-dbg fqdn cache list
- Options
- Options inherited from parent commands
- SEE ALSO

List fqdn cache contents

cilium-dbg fqdn cache - Manage fqdn proxy cache

---

## cilium-dbg fqdn cache clean — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_fqdn_cache_clean/

**Contents:**
- cilium-dbg fqdn cache clean
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg fqdn cache - Manage fqdn proxy cache

---

## cilium-operator-azure status — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_status/

**Contents:**
- cilium-operator-azure status
- Options
- SEE ALSO

Display status of operator

cilium-operator-azure - Run cilium-operator-azure

cilium-operator-azure status clustermesh - Display status of remote clusters

---

## cilium-operator-azure metrics list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure_metrics_list/

**Contents:**
- cilium-operator-azure metrics list
- Options
- SEE ALSO

List all metrics for the operator

cilium-operator-azure metrics - Access metric status of the operator

---

## cilium-dbg status — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_status/

**Contents:**
- cilium-dbg status
- Options
- Options inherited from parent commands
- SEE ALSO

Display status of daemon

---

## cilium-operator-azure — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-azure/

**Contents:**
- cilium-operator-azure
- Options
- SEE ALSO

Run cilium-operator-azure

cilium-operator-azure completion - Generate the autocompletion script for the specified shell

cilium-operator-azure hive - Inspect the hive

cilium-operator-azure metrics - Access metric status of the operator

cilium-operator-azure shell - Connect to the Cilium shell

cilium-operator-azure status - Display status of operator

cilium-operator-azure troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-operator-alibabacloud hive — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_hive/

**Contents:**
- cilium-operator-alibabacloud hive
- Options
- SEE ALSO

cilium-operator-alibabacloud - Run cilium-operator-alibabacloud

cilium-operator-alibabacloud hive dot-graph - Output the dependencies graph in graphviz dot format

---

## cilium-dbg build-config — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_build-config/

**Contents:**
- cilium-dbg build-config
- Options
- Options inherited from parent commands
- SEE ALSO

Resolve all of the configuration sources that apply to this node

---

## cilium-dbg bpf policy list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_policy_list/

**Contents:**
- cilium-dbg bpf policy list
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf policy - Manage policy related BPF maps

---

## cilium-dbg prefilter list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_prefilter_list/

**Contents:**
- cilium-dbg prefilter list
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg prefilter - Manage XDP CIDR filters

---

## cilium-dbg post-uninstall-cleanup — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_post-uninstall-cleanup/

**Contents:**
- cilium-dbg post-uninstall-cleanup
- Synopsis
- Options
- Options inherited from parent commands
- SEE ALSO

Remove system state installed by Cilium at runtime

Clean up CNI configurations, CNI binaries, attached BPF programs, bpffs, tc filters, routes, links and named network namespaces.

Running this command might be necessary to get the worker node back into working condition after uninstalling the Cilium agent.

---

## cilium-dbg recorder get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_recorder_get/

**Contents:**
- cilium-dbg recorder get
- Options
- Options inherited from parent commands
- SEE ALSO

Display individual pcap recorder

cilium-dbg recorder - Introspect or mangle pcap recorder

---

## cilium-dbg policy get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_policy_get/

**Contents:**
- cilium-dbg policy get
- Options
- Options inherited from parent commands
- SEE ALSO

Display policy node information (deprecated)

cilium-dbg policy - Manage security policies

---

## cilium-operator-alibabacloud completion bash — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_completion_bash/

**Contents:**
- cilium-operator-alibabacloud completion bash
- Synopsis
  - Linux:
  - macOS:
- Options
- SEE ALSO

Generate the autocompletion script for bash

Generate the autocompletion script for the bash shell.

This script depends on the ‘bash-completion’ package. If it is not installed already, you can install it via your OS’s package manager.

To load completions in your current shell session:

To load completions for every new session, execute once:

You will need to start a new shell for this setup to take effect.

cilium-operator-alibabacloud completion - Generate the autocompletion script for the specified shell

---

## cilium-operator-alibabacloud completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-alibabacloud_completion/

**Contents:**
- cilium-operator-alibabacloud completion
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for the specified shell

Generate the autocompletion script for cilium-operator-alibabacloud for the specified shell. See each sub-command’s help for details on how to use the generated script.

cilium-operator-alibabacloud - Run cilium-operator-alibabacloud

cilium-operator-alibabacloud completion bash - Generate the autocompletion script for bash

cilium-operator-alibabacloud completion fish - Generate the autocompletion script for fish

cilium-operator-alibabacloud completion powershell - Generate the autocompletion script for powershell

cilium-operator-alibabacloud completion zsh - Generate the autocompletion script for zsh

---

## cilium-dbg bpf multicast group — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_multicast_group/

**Contents:**
- cilium-dbg bpf multicast group
- Options
- Options inherited from parent commands
- SEE ALSO

Manage the multicast groups.

cilium-dbg bpf multicast - Manage multicast BPF programs

cilium-dbg bpf multicast group add - Add a multicast group.

cilium-dbg bpf multicast group delete - Delete a multicast group.

cilium-dbg bpf multicast group list - List the multicast groups.

---

## cilium-operator-aws troubleshoot kvstore — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_troubleshoot_kvstore/

**Contents:**
- cilium-operator-aws troubleshoot kvstore
- Options
- SEE ALSO

Troubleshoot connectivity towards the etcd kvstore

cilium-operator-aws troubleshoot - Run troubleshooting utilities to check control-plane connectivity

---

## cilium-dbg policy wait — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_policy_wait/

**Contents:**
- cilium-dbg policy wait
- Options
- Options inherited from parent commands
- SEE ALSO

Wait for all endpoints to have updated to a given policy revision

cilium-dbg policy - Manage security policies

---

## cilium-dbg bpf nat retries — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_bpf_nat_retries/

**Contents:**
- cilium-dbg bpf nat retries
- Options
- Options inherited from parent commands
- SEE ALSO

cilium-dbg bpf nat - NAT mapping tables

cilium-dbg bpf nat retries flush - Reset the NAT retries histogram

cilium-dbg bpf nat retries list - Show the NAT retries histogram

---

## cilium-dbg endpoint get — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_endpoint_get/

**Contents:**
- cilium-dbg endpoint get
- Examples
- Options
- Options inherited from parent commands
- SEE ALSO

Display endpoint information

cilium-dbg endpoint - Manage endpoints

---

## cilium-dbg lrp list — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-dbg_lrp_list/

**Contents:**
- cilium-dbg lrp list
- Options
- Options inherited from parent commands
- SEE ALSO

List local redirect policies

cilium-dbg lrp - Manage local redirect policies

---

## cilium-operator-aws completion — Cilium 1.18.5 documentation

**URL:** https://docs.cilium.io/en/stable/cmdref/cilium-operator-aws_completion/

**Contents:**
- cilium-operator-aws completion
- Synopsis
- Options
- SEE ALSO

Generate the autocompletion script for the specified shell

Generate the autocompletion script for cilium-operator-aws for the specified shell. See each sub-command’s help for details on how to use the generated script.

cilium-operator-aws - Run cilium-operator-aws

cilium-operator-aws completion bash - Generate the autocompletion script for bash

cilium-operator-aws completion fish - Generate the autocompletion script for fish

cilium-operator-aws completion powershell - Generate the autocompletion script for powershell

cilium-operator-aws completion zsh - Generate the autocompletion script for zsh

---
